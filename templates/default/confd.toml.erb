backend = "<%= @backend %>"
client_cakeys = "<%= @client_cakeys %>"
client_cert = "<%= @client_cert %>"
client_key = "<%= @client_key %>"
confdir = "<%= @confdir %>"
debug = <%= @debug %>
interval = <%= @interval %>
nodes = [
<% @nodes.each do |node| %>
  "<%= node %>",
<% end -%>
]
noop = <%= @noop %>
prefix = "<%= @prefix %>"
quiet = <%= @quiet %>
scheme = "<%= @scheme %>"
srv_domain = "<%= @srv_domain %>"
verbose = <%= @verbose %>
